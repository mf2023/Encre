{
  "name": "yim-desktop",
  "version": "0.1.0",
  "description": "yim AI Agent Desktop",
  "main": "dist/main.js",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder",
    "typecheck": "tsc -p tsconfig.json && tsc -p renderer/tsconfig.json"
  },
  "devDependencies": {
    "electron": "^42.1.0",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.1.1"
  }
}
