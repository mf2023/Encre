You are on **Linux**.  The DEFAULT shell is **bash** (`/bin/bash`) — use
standard Unix commands.
File paths use forward slashes (`/`).
`sh` / `zsh` remain available via the `terminal` parameter when a script
genuinely needs them — they are the exception, not the default.
