[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ea-tool-vlm-computer-use"
version = "0.4.3"
description = "EA (Encre Agent) — system-default vlm_computer_use tool(s)."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "Dunimd Team", email = "dunimd@outlook.com"}]
keywords = ["ea", "agent", "tool", "system-default"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
]
dependencies = ["encre-harness>=0.4.3"]

[project.entry-points."ea.plugins"]
vlm_computer_use = "ea_tool_vlm_computer_use.plugin:create_plugin"

[tool.ea]
tier = "system-default"

[tool.setuptools.packages.find]
where = ["."]
include = ["ea_tool_vlm_computer_use*"]

[tool.setuptools.package-data]
"ea_tool_vlm_computer_use" = ["skills/**/*.md"]
